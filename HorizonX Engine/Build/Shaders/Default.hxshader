[HXShader Version 1.0 GLSLVersion 330]

vertex {
    #version $GLSLVersion

    layout(location = 0) in vec3 vertexPosition_modelspace;

    void main() {
        gl_Position.xyz = vertexPosition_modelspace;
        gl_Position.w = 1.0;
    }
}

fragment{
    #version $GLSLVersion

    out vec4 color;

    void main() {
        color = vec3(1, 0, 1);
    }
}